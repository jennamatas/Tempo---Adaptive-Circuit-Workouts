# Tempo environment variables
# Copy to `.env` and fill in to point the app at your own Supabase project.
# If these are left unset, the app falls back to the bundled demo backend so a
# fresh clone runs immediately.

# Public (safe to ship to the client)
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

node_modules/
.expo/
dist/
coverage/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
# Keep brand assets (app icons, favicon, splash, logo source) in the repo so a
# fresh clone builds; ignore anything else dropped into assets/.
assets/*
!assets/images/
!assets/brand/
metro-cache/
.metro-cache-*/




ios
android

# macOS
.DS_Store

# Temporary files created by Metro to check the health of the file watcher
.metro-health-check*

expo-env.d.ts

# Local environment files — never commit real secrets.
# Ignore every .env variant (.env, .env.local, .env.seed.local, ...) but keep
# the committed templates so contributors know which vars to set.
.env
.env.*
!.env.example
!.env.seed.example

# Local agent / tooling config
.claude
.vercel
.mcp.json
.env.local

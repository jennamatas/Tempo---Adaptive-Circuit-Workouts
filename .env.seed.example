# Seed/admin secrets TEMPLATE — copy to `.env.seed.local` and fill in.
# Server-only. NEVER commit real values and NEVER ship these to a client.
# Used only by out-of-band seed/admin scripts, never loaded by the Expo app.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
POSTGRES_URL=postgres://postgres:password@host:5432/postgres
